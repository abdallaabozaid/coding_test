import 'package:flutter/material.dart';

class ClassesTap extends StatelessWidget {
  const ClassesTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
