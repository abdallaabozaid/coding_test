import 'package:flutter/material.dart';

class CommunityTap extends StatelessWidget {
  const CommunityTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
